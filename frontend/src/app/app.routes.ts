/**
 * @fileoverview Application routing configuration.
 * Currently empty as the application uses a single-page layout within App component.
 */

import { Routes } from '@angular/router';

export const routes: Routes = [];
